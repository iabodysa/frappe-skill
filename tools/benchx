#!/usr/bin/env python3
"""The name on PATH. Kept separate from the module so the module stays importable by tests."""
import importlib
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))

raise SystemExit(importlib.import_module("benchx").main())
